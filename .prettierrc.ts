import type { Config } from 'prettier';

const config: Config = {
    printWidth: 100,
    tabWidth: 4,
    useTabs: false,
    semi: true,
    singleQuote: true,
    bracketSpacing: true,
    arrowParens: 'always',
};

export default config;
